# SDKWork Connect Plugins

Repository-local plugins may be added here when SDKWork Connect needs local agent extensions.

Plugins must include a documented manifest when installable and must not vendor unrelated toolchains, generated SDK output, secrets, runtime data, caches, or logs.
